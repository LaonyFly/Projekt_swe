<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.fh_bielefeld.projekt_swe" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".filter"
            android:label="@string/title_activity_filter"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity
            android:name=".overview_room"
            android:label="@string/title_activity_overview_room"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity
            android:name=".overview_status"
            android:label="@string/title_activity_overview_status"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity
            android:name=".list_room"
            android:label="@string/title_activity_list_room"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
    </application>

</manifest>
